import { makeStyles } from "@mui/styles";

const boxesStyle = makeStyles(() => ({
  DimensionMeasureBox: {
    border: "1px solid #e6e6e6",
    display: "flex",
    justifyContent: "flex-end",
    borderRadius: 5,
    height: 50,
    margin: 40,
  },
}));

export default boxesStyle;
